# Force UCX for point-to-point (CUDA-aware)
pml = ucx

# Disable BTLs that can't handle GPU pointers
btl = ^vader,tcp,openib

# UCX transports: CUDA IPC + copy, shared memory, RC (RDMA)
pml_ucx_tls = cuda_ipc,cuda_copy,rc,sm
