# test-input
#
# Place input data files here.
# These will be mounted into /workspace/test-input inside the container.
#
# The current benchmark (mpi_cuda_bench) does not require external input files;
# message sizes are compiled in. Future solvers can read from this directory.
